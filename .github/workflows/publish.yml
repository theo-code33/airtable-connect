name: Publish Airtable Connect Package to npmjs
on:
  push:
    tags:
      - "v*"
jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      LAST_VERSION: ${{ steps.release.outputs.LAST_VERSION }}
      VERSION: ${{ steps.release.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v4
      - name: Install jq
        run: sudo apt-get install jq -y
      - name: Release upgrade
        run: |
          VERSION=$(echo $GITHUB_REF_NAME | cut -c 2-) && \
          jq '.version |="'$(echo $VERSION)'"' package.json > package.json.tmp && mv package.json.tmp package.json && \
          echo "Release version: $VERSION" && \
          echo "LAST_VERSION=$(jq -r '.version' package.json)" >> $GITHUB_OUTPUT && \
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      - name: Show package.json
        run: cat package.json
      - name: Commit release
        uses: EndBug/add-and-commit@v9
        with:
          add: "package.json"
          author_name: "Théo Gillet"
          author_email: "gillettheo1@gmail.com"
          message: "Release ${{ steps.release.outputs.VERSION }}"
          new_branch: main
          pull: "--rebase"
  build:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        # with:
        ##   ref: main
      - uses: actions/setup-node@v3
        with:
          node-version: "18.x"
          registry-url: "https://registry.npmjs.org"
      - name: Install dependencies
        run: npm ci
      - name: Publish package
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  remove_last_commit:
    needs: build
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Reset package version
        run: |
          LAST_VERSION=$(echo ${{ steps.release.outputs.LAST_VERSION }}) && \
          jq '.version |="'$(echo $LAST_VERSION)'"' package.json > package.json.tmp && mv package.json.tmp package.json && \
          echo "Last version: $LAST_VERSION"
      - name: Remove last commit
        uses: EndBug/add-and-commit@v9
        with:
          add: "package.json"
          author_name: "Théo Gillet"
          author_email: "gillettheo1@gmail.com"
          message: "Release ${{ env.GITHUB_REF_NAME }} failed"
          new_branch: main
